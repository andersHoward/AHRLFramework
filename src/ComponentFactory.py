"""Creates requested components with requested settings for the Map Generator."""

import yaml
import ecs.ecs.models

class ComponentFactory():

    def __init__(self):
        pass

    def ConfigureNewComponent(self, component_name, config_yaml, args):
        '''For each setting in the args list, set up the requested component.'''
        pass
