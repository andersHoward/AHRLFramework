class EntityFactory():
    """Used by map generator to get the entities it needs for a level created."""

    def __init__(self, params):
        level_params = params
        # entity_superclass = entity_superclass
        # entity_subclass = entity_subclass
        # entity = EntityManager.create_entity(self)
        # entity_component_list = {}
